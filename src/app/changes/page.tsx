import { prisma } from '@/lib/prisma';
export default async function Changes(){const rows=await prisma.menuPriceChange.findMany({include:{restaurant:true,menuItem:true},orderBy:{changeDate:'desc'}});return <div><h1>История изменений</h1><table><thead><tr><th>Ресторан</th><th>Блюдо</th><th>Старая цена</th><th>Новая цена</th><th>Старая себест.</th><th>Новая себест.</th><th>Дата</th><th>План</th><th>Комментарий</th></tr></thead><tbody>{rows.map(r=><tr key={r.id}><td>{r.restaurant.name}</td><td>{r.menuItem.name}</td><td>{r.oldSalePrice}</td><td>{r.newSalePrice}</td><td>{r.oldCostPrice}</td><td>{r.newCostPrice}</td><td>{r.changeDate.toISOString().slice(0,10)}</td><td>{r.plannedDate?.toISOString().slice(0,10)}</td><td>{r.comment}</td></tr>)}</tbody></table></div>}
